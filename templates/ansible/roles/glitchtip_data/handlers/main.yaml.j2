---
{% raw %}
- name: Restart postgresql
  ansible.builtin.systemd:
    name: "postgresql@{{ glitchtip_postgres_version }}-main"
    state: restarted

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
{% endraw %}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homeassistant-fast-local
  namespace: homeassistant
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-local
  resources:
    requests:
      storage: 10Gi
